workflows:
  pwa-build:
    name: PWA Build
  
    max_build_duration: 30
    environment:
      node: lts
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build app
        script: npm run build
